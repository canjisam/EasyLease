<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cwnu.lease.web.app.mapper.RoomInfoMapper">

    <resultMap id="RoomItemVoMap" type="com.cwnu.lease.web.app.vo.room.RoomItemVo" autoMapping="true">
        <!--房间信息的映射-->
        <id column="id" property="id"/>
        <!--公寓信息的映射-->
        <association property="apartmentInfo" javaType="com.cwnu.lease.model.entity.ApartmentInfo" autoMapping="true">
            <id column="apartment_id" property="id"/>
        </association>
        <collection property="graphVoList" ofType="com.cwnu.lease.web.app.vo.graph.GraphVo"
                    select="selectGraphListByRoomId" column="id"/>
        <collection property="labelInfoList" ofType="com.cwnu.lease.model.entity.LabelInfo"
                    select="selectLabelListByRoomId" column="id"/>
    </resultMap>

    <!--嵌套查询  嵌套结果映射不能用-->
    <select id="pageItem" resultMap="RoomItemVoMap">
        select ri.id,
               ri.room_number,
               ri.rent,
               ai.id apartment_id,
               ai.name,
               ai.introduction,
               ai.district_id,
               ai.district_name,
               ai.city_id,
               ai.city_name,
               ai.address_detail,
               ai.latitude,
               ai.longitude,
               ai.phone,
               ai.is_release
        from room_info ri left join apartment_info ai on ri.apartment_id = ai.id and ai.is_deleted = 0
        <where>
            ri.is_deleted = 0<!--未删除对象-->
            and   ri.is_release = 1<!--已发布-->
            <!--获取未入住的房间-->
            and   ri.id not in(
            <!--获取已入住的房间-->
            select
            room_id
                                from lease_agreement
                                where is_deleted = 0
                                and status in(2,5)<!--2是已签约，5是退租待确认-->)

            <!--省份的过滤条件-->
            <if test="queryVo.provinceId != null">
                and ai.province_id = #{queryVo.provinceId}
            </if>

            <!--城市的过滤条件-->
            <if test="queryVo.cityId != null">
                and ai.city_id = #{queryVo.cityId}
            </if>

            <!--区域的过滤条件-->
            <if test="queryVo.districtId != null">
                and ai.district_id = #{queryVo.districtId}
            </if>

            <!--大于等于最小租金和小于等于最大租金的过滤条件-->
            <if test="queryVo.minRent != null and queryVo.maxRent != null">
                and (ri.rent &gt;= #{queryVo.minRent} and ri.rent &lt;= #{queryVo.maxRent})
            </if>

            <!--支付方式的过滤条件-->
            <if test="queryVo.paymentTypeId != null">
            and ri.id in(
                select
                room_id
                from room_payment_type<!--支付方式和房间的关系表-->
                where is_deleted = 0
                and payment_type_id = #{queryVo.paymentTypeId}
            )
            </if>

        </where>
        <!--价格排序方式-->
        <if test="queryVo.orderType == 'desc' or queryVo.orderType == 'asc'">
            order by ri.rent ${queryVo.orderType}
        </if>

    </select>
    
    <select id="selectGraphListByRoomId" resultType="com.cwnu.lease.web.app.vo.graph.GraphVo">
        select
            name,
            url
        from graph_info
        where is_deleted = 0
        and item_type = 2
        and item_id = #{id}
    </select>
    
    <select id="selectLabelListByRoomId" resultType="com.cwnu.lease.model.entity.LabelInfo">
        select
            id,
            name,
            type
        from label_info
        where is_deleted = 0
        and id in(
        <!--从关系表中获取对应id-->select
            label_id
            from
        room_label
        where is_deleted = 0
        and room_id = #{id}
        )
    </select>
    <select id="selectMinRentByApartmentId" resultType="java.math.BigDecimal">
        select min(rent)
        from room_info
        where is_deleted = 0
          and is_release = 1
          and apartment_id = #{id}
    </select>
    <select id="pageItemByApartmentId" resultMap="RoomItemVoMap">
        select ri.id,
        ri.room_number,
        ri.rent,
        ai.id apartment_id,
        ai.name,
        ai.introduction,
        ai.district_id,
        ai.district_name,
        ai.city_id,
        ai.city_name,
        ai.address_detail,
        ai.latitude,
        ai.longitude,
        ai.phone,
        ai.is_release
        from room_info ri left join apartment_info ai on ri.apartment_id = ai.id and ai.is_deleted = 0
        where ri.apartment_id = #{id}
            and   ri.is_deleted = 0
            and   ri.is_release = 1
            and   ri.id not in(
            select
            room_id
            from lease_agreement
            where is_deleted = 0
            and status in(2,5))

    </select>
</mapper>
